// The at() method of Array instances takes an integer value and
//  returns the item at that index, allowing for positive and
//   negative integers. Negative integers count back from the last item in the array.

//the at index is used to return the item at a specific index of an array
const tryArray = [2, 3, 4, 5, 56, 5, 7];
const index = 3;

console.log(tryArray.at(index));
